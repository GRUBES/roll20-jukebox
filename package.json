{
  "name": "roll20-jukebox",
  "version": "0.1.0",
  "description": "Exposes the [Roll20](https://roll20.net/) Jukebox API for use in macros.",
  "main": "jukebox-api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:GRUBES/roll20-jukebox.git"
  },
  "keywords": [
    "roll20"
  ],
  "author": "Draico Dorath",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^1.27.14",
    "@rollup/plugin-json": "^4.0.1"
  }
}
